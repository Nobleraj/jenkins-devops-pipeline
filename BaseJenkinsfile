pipeline {
    agent any

    stages {
        stage('Build base image') {
            steps {
                script {
                    // Build the Docker image
                    docker.build('docker build -t baseimage:v1 -f BaseDockerfile ./')
                }
            }
        }
    }
    
}
