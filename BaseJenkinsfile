def userName = 'princenoble/'
pipeline {
    agent any

    stages {
        stage('Build base image') {
            steps {
                script {
                    // Build the Docker image
                   docker.build("${userName}baseimage-${imageTag}:${imageVersion}", "-f BaseDockerfile .")
                }
            }
        }
    }
    
}
